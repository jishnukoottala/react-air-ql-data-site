import React from "react";

import {BASE_URL} from "../services/baseUrl";

const FormFilter = () => {
  // React.useEffect(() => {
  //   fetch(`${BASE_URL}/latest?order_by=country`).then(res => {
  //     res.json().then(result => {
  //       console.log(result);
  //     });
  //   });
  // });

  return <div>Second challenge</div>;
};

export default FormFilter;
